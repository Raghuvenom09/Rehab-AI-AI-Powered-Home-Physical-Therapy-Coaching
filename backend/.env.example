# JWT Configuration
JWT_SECRET_KEY=your-super-secret-key-change-this-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRATION_MINUTES=1440

# Server
HOST=0.0.0.0
PORT=8000

# Model (auto-detected: cuda if available, else cpu)
# Force CPU: FORCE_CPU=true
FORCE_CPU=false
